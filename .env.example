# Environment variables for Portal Lab
# Copy this file to `.env` and adjust values as needed. Most values have
# sensible defaults. These variables are consumed by both the API and the
# frontend via Docker Compose or locally via `next dev`.

# Secret used to sign the mock JWT tokens issued during login. Even though
# tokens are mock encoded (base64 payload), the secret allows you to switch to
# proper JWT signing in the future.
JWT_SECRET=secret

# Maximum allowed size of uploaded files in bytes. The UI displays a limit of
# 5 MB and this default matches that.
MAX_FILE_SIZE=5242880

# Comma separated list of allowed MIME types for uploads. Only images are
# accepted in the demo.
ALLOWED_TYPES=image/png,image/jpeg

# Internal URL for the API. When running via docker‑compose this should be
# `http://api:4000`. When running the API standalone set this to the real
# endpoint (e.g. http://localhost:4000).
API_INTERNAL_URL=http://api:4000

# Prefix exposed to the frontend for API requests. The Next.js rewrite maps
# requests under this path to API_INTERNAL_URL.
NEXT_PUBLIC_API_BASE=/api